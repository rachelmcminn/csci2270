#include "LL.hpp"
#include <fstream>
#include <iostream>
#include <string>
using namespace std;

int main(int argc, char* argv[]){
    //object representing the tracking id's (linked list representation in LL)
    //LL linkedList;
    
}



/*
Timing operations
#include chrono or #inlcude ctime can be used to time operations
For ctime; create a variable of high precision for start time and end time and for execTime
startTime = clock();
//Put in thing your Timing
endTime = clock();
execTime = (endTime - startTime) / CLOCKS_PER_SEC;

*/


/*
chrono::time_point<std::chrono::high_resolution_clock> start, end;
start = std::chrono::high_resolution_clock::now();
// Your code here
end = std::chrono::high_resolution_clock::now();
chrono::duration<float> time_span = duration_cast<chrono::duration<float>>(end-start);
cout << time_span.count() << endl;</chrono::duration<float></float></std::chrono::high_resolution_clock>

*/